<UserControl
    x:Class="DashBoard.Controls.TextBoxControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DashBoard.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="root"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>

        <Style x:Key="BaseControl" TargetType="{x:Type FrameworkElement}">
            <Setter Property="Control.FontSize" Value="{Binding FontSize, ElementName=root}" />
            <Setter Property="Control.FontFamily" Value="{Binding FontFamily, ElementName=root}" />
        </Style>

        <Style BasedOn="{StaticResource BaseControl}" TargetType="{x:Type TextBlock}">
            <Setter Property="Text" Value="{Binding WaterMarkText, ElementName=root}" />
            <Setter Property="Foreground" Value="{Binding WaterMarkTextColor, ElementName=root}" />
            <Setter Property="Padding" Value="5,0,0,0" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
                <!--<DataTrigger Binding="{Binding Text, ElementName=txt}" Value="">
                    <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>-->
                <!--  텍스트 박스의 텍스트가 비어 있고, 포커스를 갖지 않을 때 워터마크 표시  -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Text, ElementName=txt}" Value="" />
                        <Condition Binding="{Binding IsFocused, ElementName=txt}" Value="False" />
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Visibility" Value="Visible" />
                </MultiDataTrigger>

            </Style.Triggers>

        </Style>

        <Style BasedOn="{StaticResource BaseControl}" TargetType="{x:Type TextBox}">
            <Setter Property="Text" Value="{Binding Text, ElementName=root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="Padding" Value="5,0,0,0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="VerticalContentAlignment" Value="Center" />


        </Style>
    </UserControl.Resources>

    <Grid>
        <TextBlock />
        <TextBox x:Name="txt" />
    </Grid>
</UserControl>
